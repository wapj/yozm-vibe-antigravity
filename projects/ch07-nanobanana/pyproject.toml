[project]
name = "ch07-nanobanana"
version = "0.1.0"
description = "7장: [프로젝트2] 나노바나나 프레젠테이션 자동화"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nanobanana"]
