[project]
name = "ch04-horse-racing"
version = "0.1.0"
description = "4장: [프로젝트1] 바이브코딩 맛보기 - 경마 게임 만들기"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["horse_racing"]
