[project]
name = "ch08-ai-board"
version = "0.1.0"
description = "8장: [프로젝트3] 풀스택 AI 게시판"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ai_board"]
