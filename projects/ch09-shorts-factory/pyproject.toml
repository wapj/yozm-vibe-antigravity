[project]
name = "ch09-shorts-factory"
version = "0.1.0"
description = "9장: [프로젝트4] 쇼츠 자동 생성 시스템"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["shorts_factory"]
